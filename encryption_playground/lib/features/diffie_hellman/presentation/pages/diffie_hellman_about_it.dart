import 'package:flutter/material.dart';

class DiffieHellmanAboutIt extends StatelessWidget {
  const DiffieHellmanAboutIt({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [
          Text('Diffie_hellman_about it')
        ],
      ),
    );
  }
}
